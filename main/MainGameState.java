package main;

/**
 * Different states for when game is running. Used to distinguish between different GUI behaviours and responses for
 * easier refactoring
 * @author flanagdonn
 *
 */
public enum MainGameState {
	MAIN, DISPLAY_INVENTORY
}
